#include "header.hpp"

void settings() {
    /*Vector2i mousePos;

    while (window.pollEvent(event)) {
        if (event.type == Event::Closed)
            window.close();
        if (Mouse::isButtonPressed(Mouse::Left))
            mousePos = Mouse::getPosition(window);
    }

    Text backText = createText("< Back", 10, 10, 2);

    window.clear(Color::White);
    window.draw(backText);

    if (isClickBetween(mousePos, getInitialPos(backText), getFinalPos(backText))) {
        controlPanel = 3;
    }

    // Bombas
    Text bombsText = createText("Bombs: ", windowWidth / 8, 8, 4);
    Text bombsNumText = createText(std::to_string(bombs), windowWidth * 7 / 8, 8, 4, "right");

    window.draw(bombsText);
    window.draw(bombsNumText);
    drawArrowValues(mousePos, &bombs, bombsNumText);

    // Tabuleiro

    window.display();
    */
}